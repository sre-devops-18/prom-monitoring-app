apiversion: apps/v1
kind: Daemonset
metadata:
  name: my-daemonset
  labels:
    app: node-info
spec:
  selector:
    matchLabels:
      app: node-info
  template:
    metadata:
      labels:
        app: node-info
    spec:
      containers:
      - name: prometheus
        image: prom/prometheus:latest
        ports:
        - containerport: 9090
